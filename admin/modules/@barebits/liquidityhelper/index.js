// Bitcart admin Nuxt-module entry. Bitcart auto-loads every
// modules/<scope>/<name>/index.js at admin boot; ours has no
// imperative startup logic — the route below is registered via
// config/routes.js.
export default async function () {}
