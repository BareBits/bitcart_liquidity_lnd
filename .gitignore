user_config.py
*.sqlite
*.db
*.log
*.iml
*.idea
# Ignore generated data JSON (e.g. default_channel_partners.json from
# node_db_to_json.py, wallet-debug export bundles) by default, but keep
# the plugin packaging files that Bitcart needs to install/build the
# plugin (the backend manifest and the admin module's package.json).
*.json
!/manifest.json
!/admin/modules/@barebits/liquidityhelper/package.json
.venv/
venv/
__pycache__/
.pytest_cache/
*/.venv/*
*/venv/*
*/.idea/*
*/*.idea
*/__pycache__/*
*/.pytest_cache/*
bitcart-docker
lightning_helpers_helpers.py
/test.py
/test2.py
/test3.py
/test4.py
/test5.py

# Pre-built Lightning Labs loop daemon binaries. ~130 MB on disk and
# downloaded on first use by swap_providers.ensure_loop_binaries(),
# so they should never live in version control.
loop_bin/

# Python build / coverage / cache artefacts
*.pyc
.coverage
.coverage.*
liquidityhelper.log
liquidityhelper.log.*
decisions.log
